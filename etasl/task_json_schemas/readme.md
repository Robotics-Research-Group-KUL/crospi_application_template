# Information to the user

This folder will remain empty until ``colcon build`` is called to build this package.

After that JSON schema files will be automatically generated in this directory, taking into account the parameters declared in the task specifications (located in the directory `../task_specifications`).